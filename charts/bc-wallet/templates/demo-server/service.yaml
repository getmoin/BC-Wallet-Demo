{{- if .Values.demo_server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.demo_server.name }}
  namespace: {{ .Values.global.namespaceOverride | default .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.demo_server.name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- if .Values.demo_server.labelOverride }}
    app.kubernetes.io/component: {{ .Values.demo_server.labelOverride }}
    {{- else }}
    app.kubernetes.io/component: demo-server
    {{- end }}
spec:
  type: {{ .Values.demo_server.service.type }}
  ports:
    - port: {{ .Values.demo_server.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ .Values.demo_server.name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- if .Values.demo_server.labelOverride }}
    app.kubernetes.io/component: {{ .Values.demo_server.labelOverride }}
    {{- else }}
    app.kubernetes.io/component: demo-server
    {{- end }}
{{- end }} 