{{- if .Values.demo_web.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.demo_web.name }}
  namespace: {{ .Values.global.namespaceOverride | default .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.demo_web.name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- if .Values.demo_web.labelOverride }}
    app.kubernetes.io/component: {{ .Values.demo_web.labelOverride }}
    {{- else }}
    app.kubernetes.io/component: demo-web
    {{- end }}
spec:
  type: {{ .Values.demo_web.service.type }}
  ports:
    - port: {{ .Values.demo_web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ .Values.demo_web.name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- if .Values.demo_web.labelOverride }}
    app.kubernetes.io/component: {{ .Values.demo_web.labelOverride }}
    {{- else }}
    app.kubernetes.io/component: demo-web
    {{- end }}
{{- end }} 